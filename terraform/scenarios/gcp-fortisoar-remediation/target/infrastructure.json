{
  "context": {
    "global": {
      "environment"          : "target",
      "deployment"           : "${ deployment }"
    },
    "gcp": {
      "region"               : "${ gcp_region }",
      "project_id"           : "${ gcp_project }"
    }
  }
}